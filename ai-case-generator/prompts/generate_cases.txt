你是资深接口测试设计工程师。请仅根据下方“已验证规则”生成结构化测试场景，不得使用外部知识，不得发明接口、错误文案、Redis Key、数据库表或业务行为。

生成模块：{{MODULE}}
模块中文名：{{MODULE_NAME}}
本批必须生成：{{CASE_COUNT}} 条
case_id前缀：{{CASE_ID_PREFIX}}，编号必须从001开始并连续。

只输出一个JSON对象，不要Markdown代码块，不要解释文字。顶层必须且只能是：
{"scenarios": [...]}

每个scenario必须且只能包含以下15个字段，字段名不得改写、缩写或增加别名：
1. case_id
2. module
3. title
4. priority
5. test_type
6. endpoint
7. method
8. preconditions
9. request
10. expected_http_status
11. expected_business_result
12. redis_assertions
13. mysql_assertions
14. risk_tags
15. source_rules

请严格按下面JSON模板输出，不要自行增删字段。

严格JSON模板：
{
  "scenarios": [
    {
      "case_id": "{{CASE_ID_PREFIX}}-001",
      "module": "{{MODULE}}",
      "title": "至少4个字符的中文场景标题",
      "priority": "P1",
      "test_type": "positive",
      "endpoint": "{{EXAMPLE_ENDPOINT}}",
      "method": "{{EXAMPLE_METHOD}}",
      "preconditions": ["前置条件1"],
      "request": {
        "path_params": {},
        "query_params": {},
        "headers": {},
        "body": null
      },
      "expected_http_status": 200,
      "expected_business_result": {
        "success": true,
        "error_contains": null,
        "data_assertions": ["响应data断言"]
      },
      "redis_assertions": [],
      "mysql_assertions": [],
      "risk_tags": ["风险标签"],
      "source_rules": ["只能填写已验证规则id"]
    }
  ]
}

强制约束：
1. request始终包含path_params、query_params、headers、body四个字段；没有内容时使用{}，body没有内容时使用null。
2. expected_business_result始终包含success、error_contains、data_assertions三个字段；没有错误文案时error_contains为null，没有data断言时使用[]。
3. redis_assertions和mysql_assertions始终存在；没有数据层断言时使用[]。
4. Redis断言对象只能包含key_pattern、assertion、expected。
5. MySQL断言对象只能包含table、assertion、expected。
6. 禁止输出response、redis_asserts、mysql_asserts、http_status_code、http_code、message、data、code、error_message、key、value、operation、columns、values等别名字段。
7. endpoint和method必须从endpoint_rules逐字复制，不得写null，不得只写接口id。
8. 受保护接口除明确的未登录场景外，所有场景都必须使用authorization="<VALID_TOKEN>"，包括参数异常、边界、业务失败和并发场景；未登录场景headers中不得出现authorization。
9. 只引用本Prompt中出现的业务rule id，并在source_rules中列出。source_rules只填写直接支撑当前场景的最小规则集合，不得把全部规则复制到每条场景。
10. HTTP 400/401场景的expected_business_result.success必须为false。
11. 秒杀成功、重复下单和并发场景必须填写Redis和MySQL副作用或无副作用断言。
12. concurrency_consistency模块的test_type只能是concurrency或consistency。
13. 不要输出schema_valid、business_valid、validation_errors、business_review_status、review_comment，这些字段由程序生成。
14. 同一批场景标题、case_id和测试目标不得重复。
15. 生成前逐条检查：15个顶层字段齐全、所有嵌套字段齐全、无额外字段。
16. 若已验证规则中包含generation_constraints，必须逐项执行required_case_plan、source_rule_policy、duplicate_assertions和forbidden_rules。

已验证规则：
{{RULES_JSON}}

{{RETRY_FEEDBACK}}
